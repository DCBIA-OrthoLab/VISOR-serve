fastapi
anyio
uvicorn[standard]
python-multipart
pydantic-settings
pandas
scikit-learn
lightgbm
joblib
openpyxl
odfpy
# Medical image I/O and resampling (AMASSS, and any future imaging tool).
numpy
SimpleITK
# ALI's CBCT engine: itk resamples the volumes to the two spacings the agents
# walk. Imported lazily like the stack below.
itk
# DICOM series ASO's CBCT mode cannot read through SimpleITK's GDCM reader.
# Imported lazily (tools/ASO/src/cbct/dicom.py): every other ASO mode works
# without it.
dicom2nifti
# Heavy inference stack (AMASSS, ALI). Imported LAZILY inside the functions
# that use it (see tools/AMASSS/src/nnunet_runner.py and
# tools/ALI/src/ALI_CBCT/brain.py), so the server still boots when it is
# missing -- only the tools needing it fail, with an actionable message.
# The deployment image already ships torch built against its CUDA version;
# pip sees it satisfied and skips it. Never pin or force-reinstall torch here:
# that would shadow the CUDA build with a CPU-only wheel.
vtk
torch
nnunetv2
# PINNED, and the pin is load-bearing: monai must ask for a torch the image
# already satisfies, or an unpinned entry lets pip replace the CUDA build on
# every container start -- the exact "shadow the CUDA build" failure the note
# above warns about. 1.6.0 requires torch>=2.8.0, which the lab-ai:2026.08
# image (torch 2.8.0+cu128) ships, so pip leaves torch alone. It is also the
# monai the image already carries (dragged in by shapeaxi): pinning anything
# older would shadow it with a downgrade in the pip --user layer at every
# start. Bump only together with an image rebuild (see ALI_PORT_CONTEXT.md 4).
monai==1.6.0

# NOT LISTED, and deliberately so: pytorch3d and shapeaxi, which ALI's IOS
# engine and CrownSeg need. pytorch3d has no PyPI distribution at all, so it
# is compiled from source INTO the deployment image (see the LabEnv repo's
# Dockerfile) against its torch/CUDA build; shapeaxi is baked in right after
# it, both because it declares pytorch3d as a hard dependency (pip would try
# to resolve it from PyPI here and fail) and because its own deps (lightning,
# mlflow, diffusers) are minutes of install per container start. Listing
# either here would at best reinstall what the image already ships and at
# worst shadow the CUDA torch build. Both are imported lazily; since
# lab-ai:2026.08 the IOS engine actually runs. See ALI_PORT_CONTEXT.md 4.
